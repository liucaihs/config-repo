spring:
  application:
    name: bookstore-book-center
server:
  port: 8001
  
eureka:
  client:
    service-url:
      defaultZone: http://localhost:1001/eureka/
    
feign:
  compression:
    response:
      enabled: true     
      
from: dev-environment-1.0...
